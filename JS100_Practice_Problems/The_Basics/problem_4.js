console.log('5' + 10); 
/*
  This returns 510 because of implicit type coercion. If one of the values is a string, then the other value will be converted into a string as well. When + is used with two strings, the arithmetic operation isn't performed.
*/